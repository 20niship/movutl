# UI 改善リスト (todo_ui)

目標: **現在のドックレイアウトは維持**し、(1) **タイムラインの細部の見た目・操作の改善**、(2) **メニューバーの拡充**、(3) **ステータスラインへの操作ログ表示**を優先する。AE/Premiereの要素は細部の参考にとどめ、操作感はAviUtl方式(タイムラインは現行構造を維持)。
調査元: 現UIのスクリーンショット(test.exo 読み込み後の全体)、`movutl/gui/*.cpp`、`mainmenu.cpp`、`workspace.cpp`、`status_bar.cpp`。
優先度: **P0**=見た目の不具合・基本操作の欠落 / **P1**=編集効率に直結 / **P2**=あると良い。
**今回の重点**: 第A章(タイムライン)、第B章(メニューバー)、第C章(操作ログ)。第1章以降のうちレイアウト変更を伴う項目(プロジェクトパネル新設、ワークスペース再編、ツールバー新設 等)は**対象外(保留)**。

## A. タイムライン細部改善(重点)
`movutl/gui/timeline.cpp` / `timeline_window.cpp` が対象。レイアウトは変えない。

### A1. 定規・プレイヘッド
- [x] **P0** 定規がフレーム番号のみで、プレイヘッド近傍の数字と重なる(`5|0`)。プレイヘッド側にフレーム/時間のバッジを出し、重なる目盛りラベルは間引く
- [x] **P0** 目盛りの粒度をズームに応じて自動調整(主目盛/副目盛の2段階、ラベル最小間隔を確保)
- [x] **P1** 定規の表記切替(フレーム / タイムコード hh:mm:ss:ff)。見出し部の `00:00:17:08` と定規の単位を一致させる
- [x] **P1** 定規クリック/ドラッグでのシーク(スクラブ)時に音声波形/ビューアが追従、プレイヘッドの掴みやすさ(ヒット領域拡大、ホバー強調) ※バッジのホバー強調とヒット領域拡大を実装
- [ ] **P2** プレイヘッドを表示範囲へ自動追従(再生中)、`Home/End` で先頭・末尾

### A2. オブジェクト(クリップ)の見た目
- [x] **P0** 短いオブジェクトで名前が潰れて重なる(`tetext`)。**クリップ矩形でテキストをクリップ**し、幅不足なら省略記号/非表示、ホバーでツールチップ(名前・種別・開始/終了/長さ)
- [x] **P0** 隣接オブジェクトの境界が判別しづらい。1〜2pxの境界線・角丸・ハンドル(左右端)の視認性
- [x] **P1** **選択の強調**(枠+明るさ)と複数選択の表示。インスペクタ表示中のオブジェクトを明示
- [x] **P1** 種別ごとの色の整理(映像/音声/テキスト/図形/制御/カメラ)と、カスタム色の反映(`custom_color_`)、無効(`active_=false`)は網掛け/半透明
- [x] **P1** オブジェクト上にアイコン(種別)+名前を左寄せで表示。音声は波形(実装済み)、動画/画像は**先頭サムネイル** ※動画/画像のサムネイルは未実装
- [ ] **P1** グループ制御の影響範囲(実装済みのハイライト)の見た目の洗練、グループ配下オブジェクトのインデント/連結線
- [x] **P1** 中間点/キーフレームのマーカー表示(エクソのトラックバー移動方式に対応)
- [ ] **P2** フェード(音量/不透明度)のハンドル、ループ/速度変更のバッジ(`×2.0` 等)

### A3. レイヤー見出し
- [x] **P0** レイヤー名が暗く判読しづらい。コントラスト調整、見出し幅のリサイズ、長い名前の省略
- [x] **P0** 見出しに **ロック / 表示(目) / 音声ミュート / ソロ** を並べる(現状は目のみ)。押下状態の視覚差を明確に ※目=`TrackLayer::active`(ミュート兼用)とソロを実装。ロックは変数を増やさない方針のため見送り
- [x] **P1** レイヤー名のダブルクリック編集、色ラベル、行高さの変更(S/M/L) ※編集(既存)と行高さS/M/Lを実装。色ラベルは未実装
- [x] **P1** 空レイヤーの行の見た目(交互背景の濃淡、選択レイヤー行のハイライト)
- [ ] **P2** レイヤー数を可変に(現状10固定に見える)、レイヤーの並べ替え(ドラッグ)

### A4. 操作性
- [x] **P1** タイムライン上部に小さな操作バー: 再生/停止/前後フレーム、スナップ切替、ズームスライダ、追加(`＋`)。`Ctrl+ホイール`でズーム、`Shift+ホイール`で横スクロール
- [x] **P1** **スナップ**(プレイヘッド/他オブジェクト端/マーカー)と、スナップ位置の縦ガイド線表示 ※Alt押下で一時無効。縦ガイド線表示あり
- [x] **P1** **範囲選択(ラバーバンド)**、複数選択のまとめて移動・削除
- [x] **P1** 分割(プレイヘッド位置、`S`)、複製(`Ctrl+D`)、リップル削除、右クリックメニュー(現在位置に追加・貼り付け) ※右クリックメニューの分割/複製/削除/有効切替を実装。リップル削除・ショートカットは未実装
- [x] **P1** ドラッグ中の情報表示(開始/終了フレーム、移動量)、移動先レイヤーの重なりプレビュー、重なり時の挙動を明確化 ※開始/終了/長さの表示を実装。移動先の重なりプレビューは未実装
- [x] **P1** コンポ範囲(青バー)の意味をツールチップと凡例で明示し、端のハンドル操作を分かりやすく
- [ ] **P2** マーカー(コメント付き)、ミニマップ、タイムライン設定(グリッド/スナップ間隔)ダイアログ

### A5. 複数コンポのタブ(`timeline_window.cpp`)
- [x] **P1** タブの閉じる/並べ替え/名前変更、未保存表示。`＋` ボタンのメニュー化(新規コンポ/既存を開く) ※閉じる/名前変更/＋メニューを実装。並べ替えはcompos_の順序変更が必要なため未実装
- [ ] **P2** タブ右クリック(複製、コンポ設定)

## B. メニューバー拡充(重点)
現状は `ファイル`(新規/開く/保存/名前を付けて保存/エクスポート/終了)と `表示`(ツールバー/ステータスバー/ルーラー/スタイル)のみ(`mainmenu.cpp`)。項目とショートカット表示を実装状態と一致させる。

- [ ] **P0** **`編集`**: 元に戻す `Ctrl+Z` / やり直し `Ctrl+Y` / 切り取り・コピー・貼り付け / 複製 `Ctrl+D` / 削除 `Del` / 全選択 `Ctrl+A` / 選択解除。※Undo/Redo基盤の有無を先に確認(無ければ設計から)
- [ ] **P0** **`ファイル`の拡充**: 最近使ったファイル、EXOを読み込む(既存`import_exo`をメニューへ)、素材を読み込む(動画/画像/音声)、プロジェクトを閉じる、環境設定
- [ ] **P1** **`オブジェクト`**(またはレイヤー): 追加(テキスト/画像/動画/音声/図形/MIDI/フレームバッファ/グループ制御/カスタム/コンポ参照 — ツールパネルと同一定義を共有)、分割、複製、グループ化、レイヤーの挿入/削除/上下移動、ロック/ミュート
- [ ] **P1** **`エフェクト`**: 選択オブジェクトへ追加(フィルタ一覧をカテゴリ別に自動生成、AviUtlスクリプト`.anm`も含む)、エフェクトの有効/無効・削除
- [ ] **P1** **`コンポジション`**: 新規/複製/削除、コンポ設定(`composition_settings.cpp`)、範囲設定、フレームキャッシュのクリア
- [ ] **P1** **`表示`の拡充**: ズーム(拡大/縮小/フィット/100%)、セーフマージン・グリッド・ガイド、ビューアの表示項目、タイムライン(スナップ/コンポ範囲/定規の単位)、**`ウィンドウ`**(Viewer/タイムライン/エフェクト制御/ツール/開発者/FFT/ピアノロールの表示切替、ワークスペースのリセット)
- [ ] **P1** **`再生`**: 再生/停止 `Space`、前/次フレーム `←/→`、先頭/末尾 `Home/End`、ループ、範囲プレビュー
- [ ] **P1** **`ヘルプ`**: ショートカット一覧(`shortcuts.lua`から自動生成)、バージョン情報、ログを開く
- [ ] **P1** メニュー項目の**有効/無効制御**(選択が無いときの無効化、エクスポート中の無効化は実装済み)と、チェック/ラジオ状態の表示
- [ ] **P1** メニューとコマンドの単一の定義元: `register_command`(コマンド名/説明/ショートカット)からメニュー項目を生成し、ショートカット表示と実際のキーバインドがずれないようにする
- [ ] **P2** コマンドパレット(`Ctrl+Shift+P`)、Luaからのメニュー拡張(ユーザースクリプトが項目を追加できるAPI)

## C. ステータスラインの操作ログ(重点)
現状: `(無題)`、カーソル座標、書き出し進捗、`FPS`のみ(`status_bar.cpp`)。

- [ ] **P0** **直近の操作ログ**を1行で表示(例: `オブジェクトを追加: テキスト "text"`、`移動: layer 3 → 4`、`EXOを読み込み: 26オブジェクト`、`保存しました: foo.mu`)。時間経過で薄くなる/数秒で消える
- [ ] **P0** ログの発行元を一本化: コマンド実行(`mCommand`)・Undo/Redo・ファイル入出力・エラー/警告が共通の `push_status_log(level, message)` を呼ぶ(ロガー `LOG_F` とは別に、ユーザー向けの短い文)
- [ ] **P1** レベル別の色/アイコン(情報/成功/警告/エラー)。エラーは自動で消さず、クリックで詳細
- [ ] **P1** ステータスラインのクリックで**ログ履歴のポップアップ**(直近100件、時刻付き、コピー可)。`ヘルプ > ログを開く` からも開く
- [ ] **P1** 実行中の長い処理(キャッシュ作成/書き出し/EXO読み込み)の進捗表示との共存(現状の書き出し進捗表示を統合)
- [ ] **P1** 元に戻す/やり直し時のログ(`元に戻す: 移動`)
- [ ] **P1** 左からの構成整理: `[保存状態●] プロジェクト名` / 中央: 操作ログ / 右: フレーム・時間・選択数・ズーム。UI描画FPSは開発者向けとして既定で隠す
- [ ] **P2** ログのレベルフィルタ、ファイル出力、Luaからの `movutl.status(...)` API

## 1. スクショから確認した問題

### 全体・ウィンドウ
- [ ] **P0** ウィンドウタイトルが `ImGui OpenGL3 example` のまま(サンプルのデフォルト)。`movutl - <プロジェクト名>` にする
- [ ] **P0** メインメニューが `ファイル` `表示` のみ。`編集`(元に戻す/やり直し/コピー/貼り付け/削除/複製/全選択)、`レイヤー`、`エフェクト`、`コンポジション`、`ウィンドウ`(パネルの表示切替)、`ヘルプ` が無い
- [ ] **P1** ツールバー(選択/移動/回転/拡大/基点/スナップ/グリッド切替)が無い。`表示`メニューの「ツールバー」項目は空(`mainmenu.cpp:51`)
- [ ] **P1** 既定ワークスペースが開発者向け(`開発者ウィンドウ`+`FFT` が左下に大きく常駐)。編集用/開発用でワークスペースを分け、既定は編集用にする
- [ ] **P2** ワークスペース切替UI(編集/カラー/オーディオ/開発者)とレイアウト保存・リセット

### ツールパネル(左上)
- [ ] **P0** ラベルが切れる(`フレームバッフ…` `カスタムオブジ…` `コンポ音声参照`)。ボタン幅が不揃いで2列が揃っていない。最下段が見切れる
- [ ] **P0** `コンポ参照` と `コンポ音声参照` が同じアイコンで区別できない
- [ ] **P1** 「オブジェクト追加」パレットとして再設計: アイコン+短縮ラベル+ツールチップ、グルーピング(メディア/図形・テキスト/制御/参照)、パネル幅に応じて折り返し
- [ ] **P1** 追加時の挿入先が不明: 現在フレーム・選択レイヤーへ入る旨を明示(AviUtlの「メディアオブジェクトの追加」相当の右クリックメニューもタイムラインに)

### ビューア
- [ ] **P0** フッターの `100%`(ドラッグ値)と `100%` ボタンが重複し、`フィット` と並んで意味が不明
- [ ] **P0** コンポ内容が小さく表示され余白が多い(波形+メーターが常に下に大きく居座る)。波形は折りたたみ可能に
- [ ] **P1** **再生操作が無い**(再生/停止/前後フレーム/先頭・末尾/ループ)。`Space` `←→` `Home/End` ショートカットも含める
- [ ] **P1** ビューア上に現在のタイムコード/フレーム番号が無い(タイムラインの見出し部にしかない)
- [ ] **P1** 表示補助: 透明の市松模様、セーフマージン、グリッド、ガイド(定規からドラッグ)、中心線、画質(フル/1/2/1/4)、拡大率プリセット(25/50/100/200/フィット)
- [ ] **P1** 選択オブジェクトがビューア外/非表示フレームのとき何も表示されない旨のフィードバックが無い(ギズモはあるが未表示)
- [ ] **P2** ミニマップ/パン(ハンドツール `H`/スペースドラッグ)の案内、ズームのショートカット(`Ctrl+ホイール`)の明示

### ピアノロール
- [ ] **P1** MIDI が無くても常時ドックされ、細いバーだけ残って邪魔。MIDIオブジェクト選択時のみ表示(または別ワークスペース)

### エフェクト制御(インスペクタ)
- [ ] **P0** パネル幅が狭く、コンボ(`通常` `標準`)の右側やラベルが**見切れる**。最小幅と横スクロール回避(ラベルを左、値を右のグリッド化)
- [ ] **P0** ラベルが値の**右**にある(`位置` `基点` `拡大率(%)`)。AE/AviUtl同様に**左ラベル**へ
- [ ] **P1** `揃え` 3×3 がテキストのみで選択状態が分かりにくい。アイコン(左/中央/右×上/中/下)+選択ハイライト、基点プリセット(9点)も同じ意匠に統一
- [ ] **P1** セクションの折りたたみ(トランスフォーム/テキスト/エフェクト各種)と、項目ごとの**リセット**ボタン、ダブルクリック/`Ctrl+クリック`での数値入力
- [ ] **P1** 拡大率X/Yの**リンク(縦横比固定)**トグルが無い
- [ ] **P1** **アニメーション(キーフレーム)操作が無い**: ストップウォッチ相当、値の変化に応じた自動キー、前後キーへジャンプ。AviUtlの中間点/移動方式(直線・加減速 等)に対応するコンボ
- [ ] **P1** エフェクトの追加(ブラウザ/`＋`メニュー)、有効/無効、並べ替え(ドラッグ)、削除。現状は追加導線が見えない
- [ ] **P2** 複数選択時の一括編集(共通プロパティ)、プロパティの検索/フィルタ
- [ ] **P2** 数値入力に単位・範囲を表示し、Shift/Altで速度切替

### タイムライン(AviUtl方式を維持)
- [ ] **P0** 定規が**フレーム番号のみ**でプレイヘッド付近の数字と重なる(`5|0`)。タイムコード(hh:mm:ss:ff)とフレームの切替、目盛りの粒度自動調整
- [ ] **P0** 短いオブジェクトで名前が潰れて読めない(`tetext`)。幅に応じてクリップ/省略記号、ホバーでツールチップ
- [ ] **P0** レイヤー名が暗く判読しづらい。レイヤー見出しに **ロック/ミュート(音声)/ソロ/表示** を追加(現状は目のアイコンのみ)
- [ ] **P1** 選択オブジェクトの強調(枠/明るさ)。インスペクタとの連動が見た目で分かるようにする
- [ ] **P1** 再生操作・スナップ切替・ズームスライダ・「追加」ボタンをタイムライン上部のツールバーに配置。`Ctrl+ホイール`でズーム、`Shift+ホイール`で横スクロール
- [ ] **P1** **スナップ**(プレイヤヘッド/他オブジェクト端/マーカー/グリッド)と、**範囲選択(ラバーバンド)**、複数移動
- [ ] **P1** 分割(プレイヘッド位置で `S` 相当)、リップル削除、複製(`Ctrl+D`)、タイムライン右クリックメニュー(現在フレームに追加)
- [ ] **P1** 作業領域(ワークエリア/コンポ範囲)の視覚化と操作: 現状の青バーが何を表すか不明(`comp_start_`/`comp_end_`のハンドル説明)
- [ ] **P1** 波形サムネイル(音声)は表示済みだが、動画/画像のサムネイルが無い。クリップ先頭にサムネ
- [ ] **P1** キーフレーム/中間点をオブジェクト上にマーカー表示(インスペクタのアニメーションと連動)
- [ ] **P2** レイヤー数の可変(現状10固定に見える)、レイヤーの色ラベル、レイヤー名の編集、高さ変更
- [ ] **P2** マーカー(コメント付き)、ミニタイムライン、`Home/End/[ ]` によるトリム

### ステータスバー
- [ ] **P1** 表示が `(無題)` と `120.4 FPS (8.31 ms)` のみ。**フレーム/時間、選択数、レンダ/キャッシュ状況、ズーム倍率、保存状態(未保存●)** を出す。UI描画FPSは開発者向けなので既定では隠す
- [ ] **P2** 進捗(書き出し/キャッシュ作成)の表示をクリックで詳細に

### 見た目・一貫性
- [ ] **P1** ドックタブ構成が重い: 各パネルのヘッダに `▼` ボタン+青いタブが常時出る。タブ1枚のパネルはタイトルバーのみにする
- [ ] **P1** アイコンのサイズ・余白の統一(ツールパネルと見出しで大きさがばらばら)、アクセシビリティ(コントラスト、フォントサイズ設定)
- [ ] **P2** テーマ(ダーク/ライト、AE/AviUtl風)の切替と、パネル色の統一(`ui_styles.lua` の整理)
- [ ] **P2** ツールチップとショートカット表示(`shortcuts.lua` を`ヘルプ`から一覧表示、カスタマイズ)

## 2. コードから確認した不足(スクショ外)
- [ ] **P0** 起動引数の `.exo` が取り込めない: `lancher/main.cpp` の `open_media_file` は `.exo` 非対応(`No compatible plugin found`)。D&D/メニューの `open_file` と挙動を揃える
- [ ] **P1** ショートカット: `Ctrl+Z/Y`(元に戻す/やり直し)、`Delete`、`Ctrl+C/V/D`、再生系が定義されているか要確認(`lancher/runtime/shortcuts.lua`)。メニューに表示と実装を合わせる
- [ ] **P1** 元に戻す/やり直し(Undo/Redo)の存在自体を確認し、無ければ設計(コマンド履歴)
- [ ] **P1** 未保存の変更がある状態で閉じる/開くときの確認ダイアログ
- [ ] **P2** 最近使ったファイル、自動保存、クラッシュ復旧
- [ ] **P2** 素材の管理: **プロジェクトパネル**(読み込んだメディア一覧・サムネ・使用状況・D&Dでタイムラインへ)。AE/Premiereとの最大の差
- [ ] **P2** エフェクトブラウザ(検索付き)と、プリセット適用

## 3. 実装順の提案(重点3章を先に)
1. **C 操作ログ基盤**(`push_status_log`、コマンド/ファイル操作からの発行、ステータスラインへ表示)— 他の作業の確認にも役立つので最初
2. **B メニュー拡充**(コマンド定義元を一本化 → 編集/ファイル/オブジェクト/表示/ウィンドウ/ヘルプ)。Undo/Redoの有無を確認
3. **A タイムライン**: P0(定規の重なり、クリップ名クリップ、レイヤー見出しのロック/ミュート/ソロ、境界線) → P1(選択強調、スナップ、範囲選択、分割/複製、操作バー、サムネ)
4. 保留: 第1章以降のレイアウト変更を伴う項目

## 4. 決定事項 / 未決事項
- 決定: **現行のドックレイアウトは維持**。タイムラインは**AviUtl方式を維持**したまま細部を改善。メニューバー拡充とステータスラインの操作ログを追加
- 未決(着手前にまとめて確認したい):
  1. Undo/Redo(コマンド履歴)が現在どこまであるか。無い場合、メニューの`編集`を作る前に基盤を作るか
  2. 操作ログの粒度: すべてのコマンド/ドラッグ操作を対象にするか、ファイル操作・追加/削除・エラーなど主要なもののみか
  3. メニューの用語: AviUtl用語(オブジェクト/中間点)かAE用語(レイヤー/キーフレーム)か
  4. ソロ/ミュート/ロックの意味づけ(音声のみのソロ、ロック中のオブジェクトは選択・移動不可など)
